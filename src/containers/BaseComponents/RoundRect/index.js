import React, { memo} from 'react';

const prefixCls = 'round-rect';
const mockList = [
  {
    label: '品类'
  }
]

export default memo(() => {
  return (
    <div className={prefixCls}>

    </div>
  )
})